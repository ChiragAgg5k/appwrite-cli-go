package app

// Version is stamped at build time with -ldflags.
var Version = "0.0.0"

// ExecutableName is the binary name, and the directory preferences live under.
const ExecutableName = "appwrite"
